# 实证测试数据（2026-08-16）

本仓库插件的稳定复现证据。**若你无法复现本文的轨迹指纹与性能提升，请先安装本作者的技能懒加载插件（见文末「无法复现时」）**——实测复现依赖该插件注入的按需技能机制。

## 测试设计

同一任务（马里奥复刻），三种跑法对比：

| 跑法 | 提示词 | 模式 | 结果 |
|---|---|---|---|
| 裸提示（无插件） | 「使用html技术，复刻马里奥第一关」 | 无锚定 | standard 轨迹，13 分钟 |
| 测试1 | AIOS 流程（Auto-Pilot 注入） | **精简锚定生效** | minimal 轨迹，约 20 分钟 |
| 测试2 | 「帮我1:1还原马里奥，用html网页的形式」 | **精简锚定生效** | minimal 轨迹，约 10 分钟 |

## 思维链量化（从 opencode.db 提取，会话完整记录）

| 维度 | 裸提示（对照） | 测试1：AIOS3马里奥 | 测试2：1比1还原马里奥 |
|---|---|---|---|
| 会话 ID | ses_ffbbf8422ffe7EVXDchPhdn9Sh | ses_ff900cc1fffet5i2KoWQFPoF50 | ses_ff8ffd75affeC0PuCMFn3DWrV3 |
| reasoning 总字符 | 175,663 | 135,951 | 126,213 |
| reasoning 块数 | 4（1 块 175K 巨型） | 104 | 54 |
| 块长 p50 | 214 | 277 | 797 |
| 块长 max | **175,308（爆炸单块）** | 11,816 | 19,725 |
| 巨型块（>100k） | **1** | **0** | **0** |
| `we` | 0 | **392** | **314** |
| `let me` | **159** | **0** | **0** |
| `i`（第一人称） | 387 | 26 | 36 |
| 可见回复数 | 2 | 2 | 3 |
| 工具调用数 | 4 | 109 | 56 |
| 工具分布 | read1/bash2/write1 | bash51/write19/edit19/webfetch12/read8 | bash30/edit10/webfetch9/write6 |

## 轨迹指纹判定

- **minimal 轨迹标准**：`let me`≈0、`we` 主导（100+）、无巨型单块、短块推进（p50<1000）、可见回复少、工具调用密集、每步自检
- **standard 轨迹**（裸提示）：`let me` 高频（159）、第一人称（i=387）、175K 巨型单块、一次性写盘无自检

两个测试会话均完整命中 minimal 指纹：`let me=0`、`we` 300+、无巨型块、短块自检闭环——**稳定复现**（两种不同任务形态下均成立）。

## 行为证据（自检闭环，从思维链日志提取）

**测试1（AIOS3马里奥，工程化路线）**：
- 获取真实 1-1 关卡数据（GitHub willrobinson1/mario-level-1 ASCII 地图，208×15 格）+ 交叉核对水管位置
- 多文件架构：level.js（数据+解析）/ sprites.js（像素画）/ game.js（逻辑）
- 自检升级链：node --check → vm mock canvas 加载 → menu 跑帧 → 异步 rAF 触发 start 进 play 跑帧 → 按键模拟
- 用户反馈跳跃 bug 后定位双重根因：开始按钮抢焦点（空格触发按钮重置）+ 缺少跳跃缓冲；修复 = blur() + 跳跃缓冲 + 土狼时间
- 实现地下关卡（warp pipe 传送）

**测试2（1比1还原马里奥，精度路线）**：
- 手写完整单文件 + 用 pygame clone 坐标反推物体精确位置校准关卡（水管/阶梯/旗杆/城堡）
- Node 模拟 200 帧发现"站立被踩死"→ 实现屏幕外敌人激活机制（更贴近原版）
- 用户追问真实耗时 → 主动查 opencode.db（只读模式）给出精确数据：主任务 03:57:26→04:06:57 = 9 分 31 秒，按工具类型聚合耗时

## 复现方法

1. 安装本插件（README「安装」节），完全重启 opencode
2. 新建会话，发任一行使性任务（如「使用html技术，复刻马里奥第一关」）
3. 会话结束后从 opencode.db 提取思维链统计（reasoning 块数/词频/工具调用），对照上表指纹
4. 预期：`let me`≈0、`we` 主导、无巨型块、短块自检

## 无法复现时

若你的环境无法稳定复现上述轨迹指纹（例如思维链呈现 `let me` 高频、巨型单块），**请先安装本作者的技能懒加载插件**：

- 仓库：https://github.com/itamzxm/lazy-skill-loader
- 本地源：`~/.config/opencode/plugins/keyword-skills.ts`（按需技能注入，skills-map.json 关键词路由）
- 该插件为按需技能注入机制，与锚定插件协同（实证环境即二者同时运行）

安装后重启 opencode 再测。若仍无法复现，请附带你的会话思维链统计开 issue。
